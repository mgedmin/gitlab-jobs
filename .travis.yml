language: python
python:
  - 3.5
  - 3.6
  - 3.7
  - 3.8
jobs:
  include:
    - env: TOXENV=flake8
    - env: TOXENV=coverage
    - env: TOXENV=mypy
install:
  - pip install tox-travis
script:
  - tox
notifications:
  email: false
cache: pip
